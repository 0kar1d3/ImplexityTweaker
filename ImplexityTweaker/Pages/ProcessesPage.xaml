<Page x:Class="ImplexityTweaker.Pages.ProcessesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Processes"
      Foreground="{DynamicResource App.Text}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Управление процессами" FontSize="32" FontWeight="Bold" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <ui:Button Content="Обновить список" Appearance="Primary" Click="Refresh_Click" Margin="0,0,8,0" />
            <ui:Button Content="Открыть диспетчер задач" Appearance="Secondary" Click="TaskMgr_Click" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="KillInputBox" Width="220" Text="notepad" />
            <ui:Button Content="Завершить по имени/PID"
                       Margin="8,0,0,0"
                       Appearance="Transparent"
                       Click="Kill_Click" />
        </StackPanel>
        <ListBox x:Name="ProcessList"
                 Grid.Row="3"
                 FontFamily="Consolas"
                 FontSize="12" />
    </Grid>
</Page>
